mixin search-box(key)
    form#form-search.filter-row
        input.filter-input(type='text', name='keyword', placeholder='Tìm kiếm theo tên...', value=key)
        button.btn-search(type='submit') Tìm kiếm
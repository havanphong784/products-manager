mixin alert-success(time)
    - if (messages.success)
        .message.info
          div(
              data-time=time
              class="alert alert-success"
                  show-alert
          ) #{messages.success}
              button(class="" closeAlert) x